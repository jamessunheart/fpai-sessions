# Droplet Template

This is a standard Droplet. 

## Structure
- `api/`: Backend logic (FastAPI)
- `ui/`: Frontend (if applicable)
- `data/`: Local storage
- `tests/`: Unit tests

## Usage
1. `./run.sh` to start.

