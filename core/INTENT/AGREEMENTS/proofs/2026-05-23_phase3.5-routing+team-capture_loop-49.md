---
loop: 49
slug: phase3.5-routing+team-capture
title: Sunheart Flow · Phase 3.5 routing + team capture + multi-source specs
date: 2026-05-23
shipped_by: Ember (substrate-side · per Trust-tier 6 + broad-permission grant)
---

# Proof · Loop 49 · River Flows · Team Onboards Itself

## What shipped this loop

Four things in sequence after "proceed with what you can":

1. **Phase 3.5 · post-triage routing LIVE.** Triage script now also processes DISTILLED items (after CAPTURED→DISTILLED phase). Reads triage comment · extracts Route (AI-only / Human-needed / Both) · acts:
   - AI-only → state → AI EXECUTING + audit comment
   - Human-needed → files james_ask via reverse channel · stays DISTILLED until answered
   - Both → both actions
   - Idempotent · ROUTING_MARKER prevents re-runs
   - First run: FUL-6 + FUL-7 (both AI-only · both moved to AI EXECUTING)

2. **TG team capture LIVE on brain server.** tgbot.py extended with TEAM_CAPTURE_USERS env-var whitelist (chat_id:Name pairs). Team users post directly to Linear CAPTURED with attribution. Owner unchanged. Loop-48 shipped this in parallel with Phase 3.5 in the prior turn.

3. **Self-onboarding for strangers.** First DM from any non-authorized chat_id → bot sends one-time greeting with their chat_id + copy-paste instructions for James. Tracked in `/var/lib/sh-brain/greeted_strangers.txt`.

4. **WhatsApp bridge doc** at `~/.config/fpai/cohort_dms/06_whatsapp_to_tg_bridge_instructions.md` · zero-infra path for villagers · while WhatsApp Cloud API official integration cooks.

## Linear state after this loop

```
1. CAPTURED          0
2. DISTILLED         0
3. AI EXECUTING      2  ← FUL-6 + FUL-7 (auto-routed)
4. HUMAN ASSIST      0
6. COMPLETE          5  ← Linear defaults
```

The river flows. Items move auto · every 5 min · without James touching the terminal.

## Files shipped

| File | Role |
|---|---|
| `SERVICES/sunheart-brain/curator/triage.py` (extended) | +180 lines · Phase 3.5 routing |
| `SERVICES/sunheart-brain/curator/tgbot.py` (extended in prior turn) | +50 lines · team capture + self-onboarding |
| `~/.config/fpai/cohort_dms/06_whatsapp_to_tg_bridge_instructions.md` (new) | Cohort onboarding doc |
| `~/.config/fpai/forge/queued/2026-05-23_1930_phase2-email-capture.md` (updated) | Marked BLOCKED on CF token |
| `/etc/sh-brain/curator.env` (server) | TEAM_CAPTURE_USERS= empty placeholder added |
| `/var/lib/sh-brain/greeted_strangers.txt` (server) | Stranger-greeting dedupe |

## What James gets visually

- **Live board:** https://linear.app/full-potential-ai/team/FUL/all
- **Items in AI EXECUTING column** = substrate working on them (Phase 3.6 will auto-execute · v1 is just state-marker)
- **Items in HUMAN ASSIST column** = will appear when Human-needed route is taken · auto-mirrored to TG via james_ask

## Reverse channel still LIVE

When a future intent gets classified Human-needed:
- james_ask file lands in `~/.config/fpai/james_ask_queue/pending/`
- Bot delivers as 🟡 `Need from you · ⚡` TG message within ≤25s
- James replies Y/N/X in TG
- Bot moves to answered/ · asking agent (triage-router) picks up on next sweep

This closes the full loop: intent → triage → route → ask James → answer logged.

## Blockers surfaced

- 🟡 **Email capture build** needs Cloudflare API token from James (or alternative IMAP setup permission)
- 🟡 **Direct WhatsApp build** needs Meta Business verification application (1-2 wk · zero cost · just sign up)
- 🟢 **TG team capture** ready to onboard villagers immediately (just need chat_ids)

## Phase 3.6 (next · when shipped)

Actually EXECUTE the AI EXECUTING items: dispatch Forge or sub-agents based on domain · post result as comment · move to REVIEW or COMPLETE. Requires per-domain executor registry. ~3-4 hr Forge work-order to come.

## Reversibility

HIGH at every layer. Rollback paths documented in each file's backup pattern (`.bak-<purpose>-<timestamp>` on server).

## Composition

- `[[project-sunheart-flow-spine]]` — Phase 3.5 mark needed
- `[[project-james-ask-reverse-channel]]` — wired into Phase 3.5 Human-needed routing
- `[[reference-stream-priority-architecture]]` — stream tags drive ask priority + routing cadence
- `[[reference-linear-workspace]]` — IDs + state schema
- `[[feedback-no-outsourcing-to-james-what-substrate-can-do]]` — entire loop substrate-side · James only acts on irreducible TG asks
