# Full Potential AI - Nginx Domain Configuration
# Maps all services to professional domains with SSL

# ============================================
# FULLPOTENTIAL.COM - Main Landing Page
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name fullpotential.com www.fullpotential.com;

    location / {
        proxy_pass http://localhost:8005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# FULLPOTENTIAL.AI - Alternative Main Domain
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name fullpotential.ai www.fullpotential.ai;

    location / {
        proxy_pass http://localhost:8005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# DASHBOARD.FULLPOTENTIAL.COM - System Dashboard
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name dashboard.fullpotential.com dashboard.fullpotential.ai;

    location / {
        proxy_pass http://localhost:8002;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support for live updates
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# ============================================
# API.FULLPOTENTIAL.COM - I PROACTIVE API
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name api.fullpotential.com api.fullpotential.ai;

    location / {
        proxy_pass http://localhost:8400;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # API timeout settings
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# ============================================
# MATCH.FULLPOTENTIAL.COM - I MATCH API
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name match.fullpotential.com match.fullpotential.ai;

    location / {
        proxy_pass http://localhost:8401;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# MEMBERSHIP.FULLPOTENTIAL.COM - Membership Portal
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name membership.fullpotential.com membership.fullpotential.ai;

    location / {
        proxy_pass http://localhost:8006;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# JOBS.FULLPOTENTIAL.COM - Jobs Portal
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name jobs.fullpotential.com jobs.fullpotential.ai;

    location / {
        proxy_pass http://localhost:8008;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# REGISTRY.FULLPOTENTIAL.COM - Service Registry (Internal)
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name registry.fullpotential.com registry.fullpotential.ai;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# WHITEROCK.US - White Rock Ministry (Church Services)
# ============================================
server {
    listen 80;
    listen [::]:80;
    server_name whiterock.us www.whiterock.us;

    location / {
        proxy_pass http://localhost:8020;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# Default catch-all - redirect to main site
# ============================================
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    return 301 http://fullpotential.com$request_uri;
}
